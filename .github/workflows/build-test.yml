name: Check

on:
  pull_request:

  push:
    branches:
      - main # Check branch after merge

concurrency:
  # Only run once for latest commit per ref and cancel other (previous) runs.
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
#  check-code-style:
#    name: Code Style
#    uses: playframework/.github/.github/workflows/cmd.yml@v4
#    with:
#      cmd: sbt validateCode

  check-binary-compatibility:
    name: Binary Compatibility
    uses: playframework/.github/.github/workflows/binary-check.yml@v4

  check-docs:
    name: Docs
    uses: playframework/.github/.github/workflows/cmd.yml@v4
    with:
      cmd: sbt doc

  tests:
    name: Tests
    needs:
#      - "check-code-style"
      - "check-binary-compatibility"
      - "check-docs"
    uses: playframework/.github/.github/workflows/cmd.yml@v4
    with:
      java: 21, 17
      # Symbolic lanes resolve to exact versions in project/Dependencies.scala.
      scala: 2.13.x, 3.3.x, 3.9.x, 3.next
      cmd: |
        sbt -Dscala.version="$MATRIX_SCALA" testFull
        # Test sample applications
        if [[ "$MATRIX_SCALA" == 2.* ]]; then
          published_scala=2.13.x
        else
          published_scala=3.3.x
        fi
        sbt "++$published_scala ; publishLocal"
        pushd samples/compile-timeDI/ && sbt -Dscala.version="$MATRIX_SCALA" test && popd
        pushd samples/runtimeDI/ && sbt -Dscala.version="$MATRIX_SCALA" test && popd

  finish:
    name: Finish
    if: github.event_name == 'pull_request'
    needs: # Should be last
      - "tests"
    uses: playframework/.github/.github/workflows/rtm.yml@v4
